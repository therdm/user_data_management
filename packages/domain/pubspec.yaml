name: domain
description: A new Flutter package for providing handlers, daos, and dtos.
publish_to: none
#version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  reactiv: ^0.3.2

  services:
    path: ../services



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:

  uses-material-design: true
